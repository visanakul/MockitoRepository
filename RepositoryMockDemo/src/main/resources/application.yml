server:
    port: 8085

spring:
  datasource:
    driver-class-name: oracle.jdbc.driver.OracleDriver
    url: jdbc:oracle:thin:@localhost:1521:xe
    username: system
    password: sys
  jpa:
    show-sql: true
    properties:
      hibernate:
        jdbc:
          batch_size: 20

    hibernate:
      ddl-auto: update
